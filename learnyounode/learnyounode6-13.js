var path = require('path'),
	fs = require('fs');
	
module.exports = fileList(path, ext, function callback()) {
	// TODO HERE
	callback (err, data) {
		if (err) throw err;
		// Else do cool stuff here
	}
}